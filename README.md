This very simple program sorts images on a root directory (which is the context working directory), to folders it creates on-the-fly, with `year/month/<images>`. Written for a friend of mine.

# Usage
```
go build
./images-sorting
```
